package model;

public class UserFactory {

   /* public static IUser getUser(String type) {

        if (type.equalsIgnoreCase("ADMIN")) {
            return Admin.getInstance();

        } else if (type.equalsIgnoreCase("EMPLOYEE")) {
            return new Employee();
        }
        return null;
    }

    public static IUser getUser(Object user,String type){
        if (type.equalsIgnoreCase("ADMIN")) {
            return (Admin)user;

        } else if (type.equalsIgnoreCase("EMPLOYEE")) {
            return (Employee)user;
        }
        return null;
    }
*/

}
